<?xml version="1.0" encoding="UTF-8"?>
<!--
  Configuração mínima do core `diarios`. Schema clássico (schema.xml
  versionado no git — sem Schema API): o índice é derivado e
  reconstruível; mudança de schema = editar o arquivo + reindexar.
-->
<config>
  <luceneMatchVersion>9.7</luceneMatchVersion>

  <dataDir>${solr.data.dir:}</dataDir>
  <directoryFactory name="DirectoryFactory"
                    class="${solr.directoryFactory:solr.NRTCachingDirectoryFactory}"/>
  <schemaFactory class="ClassicIndexSchemaFactory"/>

  <updateHandler class="solr.DirectUpdateHandler2">
    <updateLog>
      <str name="dir">${solr.ulog.dir:}</str>
    </updateLog>
    <autoCommit>
      <maxTime>60000</maxTime>
      <openSearcher>false</openSearcher>
    </autoCommit>
    <autoSoftCommit>
      <maxTime>5000</maxTime>
    </autoSoftCommit>
  </updateHandler>

  <requestHandler name="/select" class="solr.SearchHandler">
    <lst name="defaults">
      <str name="echoParams">explicit</str>
      <int name="rows">10</int>
      <str name="df">texto</str>
      <!-- highlight: unified, fragmentos curtos, HTML-escapado -->
      <str name="hl.method">unified</str>
      <str name="hl.encoder">html</str>
      <int name="hl.fragsize">180</int>
    </lst>
  </requestHandler>

  <requestHandler name="/update" class="solr.UpdateRequestHandler"/>

  <requestHandler name="/admin/ping" class="solr.PingRequestHandler">
    <lst name="invariants">
      <str name="q">*:*</str>
    </lst>
  </requestHandler>
</config>
